# Vite + React + `@better-seo/react` (Wave 5 E2E)

Reference **SPA** setup: **`react-helmet-async`** + **`BetterSEOHelmet`**. From monorepo root:

```bash
npm install
npm run build
npm run test:e2e -w react-seo-vite-example
```

Requires **`@better-seo/core`** and **`@better-seo/react`** built first (root **`npm run build`**).
